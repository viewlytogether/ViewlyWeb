{
    "name": "viewly",
    "version": "1.0.0",
    "private": true,
    "workspaces": ["app/*"],
    "scripts": {
        "dev:api": "bun run --cwd app/api dev",
        "dev:web": "bun run --cwd app/web dev",
        "build": "bun run build",
        "start": "bun run start"
    }
}